class Post {
    constructor(id, imageUrl, description, postTime, authorId) {
        this.id = id;
        this.imageUrl = imageUrl;
        this.description = description;
        this.postTime = postTime;
        this.authorId = authorId;
    }
}

export default Post;